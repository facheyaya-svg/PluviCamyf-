# PluviCamyf 

PluviCam est une application web pour la gestion des données météorologiques et des alertes pour les agriculteurs.

## Installation
1. Clonez le dépôt Git : `git clone https:                                    
2. Créez un environnement virtuel : `python -m venv venv`
3. Activez l'environnement virtuel : `'environnement virtuel : `source venv/bin/activate` (sur Linux/Mac) ou `venv\Scripts\activate` (sur Windows)
4. Installez les dépendances : `pip install -r requirements.txt`
5. Créez la base de données : `flask db init` et `flask db migrate -m "Initial migration"` et `flask db upgrade`

                             
1. Activez l'`

                             
1. Activez l'environnement virtuel : `source venv/bin/activate` (sur Linux/Mac) ou `venv\Scripts\activate` (sur Windows)
2. Lancez l'application : `'application : `flask run`

                   
* Consultation des prévisions météorologiques
* Gestion des champs
* Réception d'alertes en cas de conditions météorologiques défavorables

## Auteurs
Yaya Faché 

## Licence
Ce projet est sous licence MIT.
